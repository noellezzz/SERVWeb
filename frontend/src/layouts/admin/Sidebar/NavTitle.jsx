const NavTitle = ({ item }) => {
    return (
        <div className="text-gray-500 text-sm uppercase font-semibold pt-2">
            {item.title}
        </div>
    );
};

export default NavTitle;